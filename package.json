{
  "name": "precious-meals",
  "version": "1.0.0",
  "description": "Precious Meals & Bakes Booking System",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch database/db.json --port 3000",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
